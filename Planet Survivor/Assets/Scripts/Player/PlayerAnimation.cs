using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerAnimation : MonoBehaviour
{

    /*
    Animator am;
    JoyStickMove jsm;
    SpriteRenderer sr;
    void Start()
    {
        am = GetComponent<Animator>();
        jsm = GetComponent<JoyStickMove>();
        sr = GetComponent<SpriteRenderer>();
    }

   
    void Update()
    {
        if (jsm.movementJoystick.Direction.y != 0 || jsm.movementJoystick.Direction.x != 0)
        {
            am.SetBool("IsRuning", true);
            SideChecker();
        }
        else
        {
            am.SetBool("IsRuning", false);
        }
    }

    private void SideChecker ()
    {
        if (jsm.movementJoystick.Direction.x < 0 )
        {
            sr.flipX = true;
        }
        else
        {
            sr.flipX = false;
        }
    }*/
}
